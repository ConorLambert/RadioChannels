@{  
    ViewBag.Title = "Home Page";
}

@RenderPage("~/Views/Shared/_Search.cshtml", new { Title = "Search" })

@RenderPage("~/Views/Shared/_Stations.cshtml", new { Controller = "StationsController" })

<script>      
    current_genre = undefined;  
    current_channel = undefined;    
    console.log(window.location.href);
    if (window.location.href.includes("home")) {
        history.replaceState("index", null, "/#index");
    }
    else if (!window.location.href.includes("/#index"))
        history.replaceState("index", null, "/#index");    
    findHeader();
    init();
</script>